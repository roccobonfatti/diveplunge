// Modifiche per propagare setCenter al Map
'use client';
import React, { useState } from 'react';
import TopBar from './components/TopBar';
import Map from './components/Map';

const Page = () => {
  const [center, setCenter] = useState([0, 0]);

  return (
    <div>
      <TopBar setCenter={setCenter} />
      <Map center={center} />
    </div>
  );
};

export default Page;